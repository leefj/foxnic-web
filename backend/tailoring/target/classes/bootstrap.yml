application:
  nacos: 
    #ip: 192.168.80.68:8001,192.168.80.68:8002,192.168.80.68:8003
    ip: tailoring-server:9369
    #ip: 47.92.240.43:9369
    namespace: scientific-tailoring
    username: nacos
    password: nacos

server:
  port: 1819
    
spring:
  application:
    name: service-tailoring
    description: Tailoring
  cloud:
    nacos:
      discovery:
        server-addr:  ${application.nacos.ip}
        username: ${application.nacos.username}
        password: ${application.nacos.password}
        namespace: ${application.nacos.namespace}
        group: service
      config:
        server-addr:  ${application.nacos.ip}
        username: ${application.nacos.username}
        password: ${application.nacos.password}
        namespace: ${application.nacos.namespace}      
        group: service
        file-extension: yml
        
        
